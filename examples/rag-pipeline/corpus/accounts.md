# Accounts

Two-factor authentication can be reset by support after identity verification.
A reset invalidates every active session, so the customer is signed out on all
devices and must sign in again.

Account deletion is permanent after 30 days; before then it can be undone from
the account settings page.
